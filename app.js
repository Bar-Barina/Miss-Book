const { createApp } = Vue

// import AboutPage from './pages/AboutPage.js'

const options = {
    template: `
        <section class="container">
           
        </section>
    `,
    data() {
        return {
            
        }
    },
    components: {
    
    }
}


const app = createApp(options)
app.mount('#app')